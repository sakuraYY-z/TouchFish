//
// Copyright 2020-2022 Signal Messenger, LLC.
// SPDX-License-Identifier: AGPL-3.0-only
//
import { Buffer } from 'node:buffer';
import * as Errors from './Errors.js';
export * from './Errors.js';
import { Aci, ProtocolAddress, ServiceId } from './Address.js';
export * from './Address.js';
import { CiphertextMessage, } from './CiphertextMessage.js';
export * from './CiphertextMessage.js';
import { IdentityKeyPair, PrivateKey, PublicKey } from './EcKeys.js';
export * from './EcKeys.js';
import { KEMPublicKey, SignedPreKeyRecord, } from './ProtocolTypes.js';
export * from './ProtocolTypes.js';
import * as uuid from './uuid.js';
export * as usernames from './usernames.js';
export * as io from './io.js';
export * as Net from './net.js';
export * as Mp4Sanitizer from './Mp4Sanitizer.js';
export * as WebpSanitizer from './WebpSanitizer.js';
import * as Native from './Native.js';
Native.registerErrors(Errors);
// These enums must be kept in sync with their Rust counterparts.
export var CiphertextMessageType;
(function (CiphertextMessageType) {
    CiphertextMessageType[CiphertextMessageType["Whisper"] = 2] = "Whisper";
    CiphertextMessageType[CiphertextMessageType["PreKey"] = 3] = "PreKey";
    CiphertextMessageType[CiphertextMessageType["SenderKey"] = 7] = "SenderKey";
    CiphertextMessageType[CiphertextMessageType["Plaintext"] = 8] = "Plaintext";
})(CiphertextMessageType || (CiphertextMessageType = {}));
export var Direction;
(function (Direction) {
    Direction[Direction["Sending"] = 0] = "Sending";
    Direction[Direction["Receiving"] = 1] = "Receiving";
})(Direction || (Direction = {}));
// This enum must be kept in sync with sealed_sender.proto.
export var ContentHint;
(function (ContentHint) {
    ContentHint[ContentHint["Default"] = 0] = "Default";
    ContentHint[ContentHint["Resendable"] = 1] = "Resendable";
    ContentHint[ContentHint["Implicit"] = 2] = "Implicit";
})(ContentHint || (ContentHint = {}));
export function hkdf(outputLength, keyMaterial, label, salt) {
    return Native.HKDF_DeriveSecrets(outputLength, keyMaterial, label, salt);
}
export class ScannableFingerprint {
    scannable;
    constructor(scannable) {
        this.scannable = scannable;
    }
    static _fromBuffer(scannable) {
        return new ScannableFingerprint(scannable);
    }
    compare(other) {
        return Native.ScannableFingerprint_Compare(this.scannable, other.scannable);
    }
    toBuffer() {
        return this.scannable;
    }
}
export class DisplayableFingerprint {
    display;
    constructor(display) {
        this.display = display;
    }
    static _fromString(display) {
        return new DisplayableFingerprint(display);
    }
    toString() {
        return this.display;
    }
}
export class Fingerprint {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static new(iterations, version, localIdentifier, localKey, remoteIdentifier, remoteKey) {
        return new Fingerprint(Native.Fingerprint_New(iterations, version, localIdentifier, localKey, remoteIdentifier, remoteKey));
    }
    displayableFingerprint() {
        return DisplayableFingerprint._fromString(Native.Fingerprint_DisplayString(this));
    }
    scannableFingerprint() {
        return ScannableFingerprint._fromBuffer(Native.Fingerprint_ScannableEncoding(this));
    }
}
/**
 * Implements the <a href="https://en.wikipedia.org/wiki/AES-GCM-SIV">AES-256-GCM-SIV</a>
 * authenticated stream cipher with a 12-byte nonce.
 *
 * AES-GCM-SIV is a multi-pass algorithm (to generate the "synthetic initialization vector"), so
 * this API does not expose a streaming form.
 */
export class Aes256GcmSiv {
    _nativeHandle;
    constructor(key) {
        this._nativeHandle = Native.Aes256GcmSiv_New(key);
    }
    static new(key) {
        return new Aes256GcmSiv(key);
    }
    /**
     * Encrypts the given plaintext using the given nonce, and authenticating the ciphertext and given
     * associated data.
     *
     * The associated data is not included in the ciphertext; instead, it's expected to match between
     * the encrypter and decrypter. If you don't need any extra data, pass an empty array.
     *
     * @returns The encrypted data, including an appended 16-byte authentication tag.
     */
    encrypt(message, nonce, associatedData) {
        return Native.Aes256GcmSiv_Encrypt(this, message, nonce, associatedData);
    }
    /**
     * Decrypts the given ciphertext using the given nonce, and authenticating the ciphertext and given
     * associated data.
     *
     * The associated data is not included in the ciphertext; instead, it's expected to match between
     * the encrypter and decrypter.
     *
     * @returns The decrypted data
     */
    decrypt(message, nonce, associatedData) {
        return Native.Aes256GcmSiv_Decrypt(this, message, nonce, associatedData);
    }
}
export class KEMSecretKey {
    _nativeHandle;
    constructor(handle) {
        this._nativeHandle = handle;
    }
    static _fromNativeHandle(handle) {
        return new KEMSecretKey(handle);
    }
    static deserialize(buf) {
        return new KEMSecretKey(Native.KyberSecretKey_Deserialize(buf));
    }
    serialize() {
        return Native.KyberSecretKey_Serialize(this);
    }
}
export class KEMKeyPair {
    _nativeHandle;
    constructor(handle) {
        this._nativeHandle = handle;
    }
    static _fromNativeHandle(handle) {
        return new KEMKeyPair(handle);
    }
    static generate() {
        return new KEMKeyPair(Native.KyberKeyPair_Generate());
    }
    getPublicKey() {
        return KEMPublicKey._fromNativeHandle(Native.KyberKeyPair_GetPublicKey(this));
    }
    getSecretKey() {
        return KEMSecretKey._fromNativeHandle(Native.KyberKeyPair_GetSecretKey(this));
    }
}
export class PreKeyRecord {
    _nativeHandle;
    constructor(handle) {
        this._nativeHandle = handle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new PreKeyRecord(nativeHandle);
    }
    static new(id, pubKey, privKey) {
        return new PreKeyRecord(Native.PreKeyRecord_New(id, pubKey, privKey));
    }
    static deserialize(buffer) {
        return new PreKeyRecord(Native.PreKeyRecord_Deserialize(buffer));
    }
    id() {
        return Native.PreKeyRecord_GetId(this);
    }
    privateKey() {
        return PrivateKey._fromNativeHandle(Native.PreKeyRecord_GetPrivateKey(this));
    }
    publicKey() {
        return PublicKey._fromNativeHandle(Native.PreKeyRecord_GetPublicKey(this));
    }
    serialize() {
        return Native.PreKeyRecord_Serialize(this);
    }
}
export class KyberPreKeyRecord {
    _nativeHandle;
    constructor(handle) {
        this._nativeHandle = handle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new KyberPreKeyRecord(nativeHandle);
    }
    static new(id, timestamp, keyPair, signature) {
        return new KyberPreKeyRecord(Native.KyberPreKeyRecord_New(id, timestamp, keyPair, signature));
    }
    serialize() {
        return Native.KyberPreKeyRecord_Serialize(this);
    }
    static deserialize(buffer) {
        return new KyberPreKeyRecord(Native.KyberPreKeyRecord_Deserialize(buffer));
    }
    id() {
        return Native.KyberPreKeyRecord_GetId(this);
    }
    keyPair() {
        return KEMKeyPair._fromNativeHandle(Native.KyberPreKeyRecord_GetKeyPair(this));
    }
    publicKey() {
        return KEMPublicKey._fromNativeHandle(Native.KyberPreKeyRecord_GetPublicKey(this));
    }
    secretKey() {
        return KEMSecretKey._fromNativeHandle(Native.KyberPreKeyRecord_GetSecretKey(this));
    }
    signature() {
        return Native.KyberPreKeyRecord_GetSignature(this);
    }
    timestamp() {
        return Native.KyberPreKeyRecord_GetTimestamp(this);
    }
}
export class SignalMessage {
    _nativeHandle;
    constructor(handle) {
        this._nativeHandle = handle;
    }
    static _new(messageVersion, macKey, senderRatchetKey, counter, previousCounter, ciphertext, senderIdentityKey, receiverIdentityKey, pqRatchet) {
        return new SignalMessage(Native.SignalMessage_New(messageVersion, macKey, senderRatchetKey, counter, previousCounter, ciphertext, senderIdentityKey, receiverIdentityKey, pqRatchet));
    }
    static deserialize(buffer) {
        return new SignalMessage(Native.SignalMessage_Deserialize(buffer));
    }
    body() {
        return Native.SignalMessage_GetBody(this);
    }
    pqRatchet() {
        return Native.SignalMessage_GetPqRatchet(this);
    }
    counter() {
        return Native.SignalMessage_GetCounter(this);
    }
    messageVersion() {
        return Native.SignalMessage_GetMessageVersion(this);
    }
    serialize() {
        return Native.SignalMessage_GetSerialized(this);
    }
}
export class PreKeySignalMessage {
    _nativeHandle;
    constructor(handle) {
        this._nativeHandle = handle;
    }
    static _new(messageVersion, registrationId, preKeyId, signedPreKeyId, baseKey, identityKey, signalMessage) {
        return new PreKeySignalMessage(Native.PreKeySignalMessage_New(messageVersion, registrationId, preKeyId, signedPreKeyId, baseKey, identityKey, signalMessage));
    }
    static deserialize(buffer) {
        return new PreKeySignalMessage(Native.PreKeySignalMessage_Deserialize(buffer));
    }
    preKeyId() {
        return Native.PreKeySignalMessage_GetPreKeyId(this);
    }
    registrationId() {
        return Native.PreKeySignalMessage_GetRegistrationId(this);
    }
    signedPreKeyId() {
        return Native.PreKeySignalMessage_GetSignedPreKeyId(this);
    }
    version() {
        return Native.PreKeySignalMessage_GetVersion(this);
    }
    serialize() {
        return Native.PreKeySignalMessage_Serialize(this);
    }
}
export class SessionRecord {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new SessionRecord(nativeHandle);
    }
    static deserialize(buffer) {
        return new SessionRecord(Native.SessionRecord_Deserialize(buffer));
    }
    serialize() {
        return Native.SessionRecord_Serialize(this);
    }
    archiveCurrentState() {
        Native.SessionRecord_ArchiveCurrentState(this);
    }
    localRegistrationId() {
        return Native.SessionRecord_GetLocalRegistrationId(this);
    }
    remoteRegistrationId() {
        return Native.SessionRecord_GetRemoteRegistrationId(this);
    }
    /**
     * Returns whether the current session can be used to send messages.
     *
     * If there is no current session, returns false.
     */
    hasCurrentState(requirePqRatio, now = new Date()) {
        return Native.SessionRecord_HasUsableSenderChain(this, requirePqRatio, now.getTime());
    }
    currentRatchetKeyMatches(key) {
        return Native.SessionRecord_CurrentRatchetKeyMatches(this, key);
    }
}
export class ServerCertificate {
    _nativeHandle;
    static _fromNativeHandle(nativeHandle) {
        return new ServerCertificate(nativeHandle);
    }
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static new(keyId, serverKey, trustRoot) {
        return new ServerCertificate(Native.ServerCertificate_New(keyId, serverKey, trustRoot));
    }
    static deserialize(buffer) {
        return new ServerCertificate(Native.ServerCertificate_Deserialize(buffer));
    }
    certificateData() {
        return Native.ServerCertificate_GetCertificate(this);
    }
    key() {
        return PublicKey._fromNativeHandle(Native.ServerCertificate_GetKey(this));
    }
    keyId() {
        return Native.ServerCertificate_GetKeyId(this);
    }
    serialize() {
        return Native.ServerCertificate_GetSerialized(this);
    }
    signature() {
        return Native.ServerCertificate_GetSignature(this);
    }
}
export class SenderKeyRecord {
    _nativeHandle;
    static _fromNativeHandle(nativeHandle) {
        return new SenderKeyRecord(nativeHandle);
    }
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static deserialize(buffer) {
        return new SenderKeyRecord(Native.SenderKeyRecord_Deserialize(buffer));
    }
    serialize() {
        return Native.SenderKeyRecord_Serialize(this);
    }
}
export class SenderCertificate {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new SenderCertificate(nativeHandle);
    }
    static new(senderUuid, senderE164, senderDeviceId, senderKey, expiration, signerCert, signerKey) {
        if (typeof senderUuid !== 'string') {
            senderUuid = senderUuid.getServiceIdString();
        }
        return new SenderCertificate(Native.SenderCertificate_New(senderUuid, senderE164, senderDeviceId, senderKey, expiration, signerCert, signerKey));
    }
    static deserialize(buffer) {
        return new SenderCertificate(Native.SenderCertificate_Deserialize(buffer));
    }
    serialize() {
        return Native.SenderCertificate_GetSerialized(this);
    }
    certificate() {
        return Native.SenderCertificate_GetCertificate(this);
    }
    expiration() {
        return Native.SenderCertificate_GetExpiration(this);
    }
    key() {
        return PublicKey._fromNativeHandle(Native.SenderCertificate_GetKey(this));
    }
    senderE164() {
        return Native.SenderCertificate_GetSenderE164(this);
    }
    senderUuid() {
        return Native.SenderCertificate_GetSenderUuid(this);
    }
    /**
     * Returns an ACI if the sender is a valid UUID, `null` otherwise.
     *
     * In a future release SenderCertificate will *only* support ACIs.
     */
    senderAci() {
        try {
            return Aci.parseFromServiceIdString(this.senderUuid());
        }
        catch {
            return null;
        }
    }
    senderDeviceId() {
        return Native.SenderCertificate_GetDeviceId(this);
    }
    serverCertificate() {
        return ServerCertificate._fromNativeHandle(Native.SenderCertificate_GetServerCertificate(this));
    }
    signature() {
        return Native.SenderCertificate_GetSignature(this);
    }
    /**
     * Validates `this` against the given trust root at the given current time.
     *
     * @see validateWithTrustRoots
     */
    validate(trustRoot, time) {
        return Native.SenderCertificate_Validate(this, [trustRoot], time);
    }
    /**
     * Validates `this` against the given trust roots at the given current time.
     *
     * Checks the certificate against each key in `trustRoots` in constant time (that is, no result
     * is produced until every key is checked), making sure **one** of them has signed its embedded
     * server certificate. The `time` parameter is compared numerically against ``expiration``, and
     * is not required to use any specific units, but Signal uses milliseconds since 1970.
     */
    validateWithTrustRoots(trustRoots, time) {
        return Native.SenderCertificate_Validate(this, trustRoots, time);
    }
}
function bridgeSenderKeyStore(store) {
    return {
        async storeSenderKey(sender, distributionId, record) {
            return store.saveSenderKey(ProtocolAddress._fromNativeHandle(sender), uuid.stringify(distributionId), SenderKeyRecord._fromNativeHandle(record));
        },
        async loadSenderKey(sender, distributionId) {
            const sk = await store.getSenderKey(ProtocolAddress._fromNativeHandle(sender), uuid.stringify(distributionId));
            return sk ? sk._nativeHandle : null;
        },
    };
}
export class SenderKeyDistributionMessage {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static async create(sender, distributionId, store) {
        const handle = await Native.SenderKeyDistributionMessage_Create(sender, uuid.parse(distributionId), bridgeSenderKeyStore(store));
        return new SenderKeyDistributionMessage(handle);
    }
    static _new(messageVersion, distributionId, chainId, iteration, chainKey, pk) {
        return new SenderKeyDistributionMessage(Native.SenderKeyDistributionMessage_New(messageVersion, uuid.parse(distributionId), chainId, iteration, chainKey, pk));
    }
    static deserialize(buffer) {
        return new SenderKeyDistributionMessage(Native.SenderKeyDistributionMessage_Deserialize(buffer));
    }
    serialize() {
        return Native.SenderKeyDistributionMessage_Serialize(this);
    }
    chainKey() {
        return Native.SenderKeyDistributionMessage_GetChainKey(this);
    }
    iteration() {
        return Native.SenderKeyDistributionMessage_GetIteration(this);
    }
    chainId() {
        return Native.SenderKeyDistributionMessage_GetChainId(this);
    }
    distributionId() {
        return uuid.stringify(Native.SenderKeyDistributionMessage_GetDistributionId(this));
    }
}
export async function processSenderKeyDistributionMessage(sender, message, store) {
    await Native.SenderKeyDistributionMessage_Process(sender, message, bridgeSenderKeyStore(store));
}
export class SenderKeyMessage {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static _new(messageVersion, distributionId, chainId, iteration, ciphertext, pk) {
        return new SenderKeyMessage(Native.SenderKeyMessage_New(messageVersion, uuid.parse(distributionId), chainId, iteration, ciphertext, pk));
    }
    static deserialize(buffer) {
        return new SenderKeyMessage(Native.SenderKeyMessage_Deserialize(buffer));
    }
    serialize() {
        return Native.SenderKeyMessage_Serialize(this);
    }
    ciphertext() {
        return Native.SenderKeyMessage_GetCipherText(this);
    }
    iteration() {
        return Native.SenderKeyMessage_GetIteration(this);
    }
    chainId() {
        return Native.SenderKeyMessage_GetChainId(this);
    }
    distributionId() {
        return uuid.stringify(Native.SenderKeyMessage_GetDistributionId(this));
    }
    verifySignature(key) {
        return Native.SenderKeyMessage_VerifySignature(this, key);
    }
}
export class UnidentifiedSenderMessageContent {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new UnidentifiedSenderMessageContent(nativeHandle);
    }
    static new(message, sender, contentHint, groupId) {
        return new UnidentifiedSenderMessageContent(Native.UnidentifiedSenderMessageContent_New(message, sender, contentHint, groupId));
    }
    static deserialize(buffer) {
        return new UnidentifiedSenderMessageContent(Native.UnidentifiedSenderMessageContent_Deserialize(buffer));
    }
    serialize() {
        return Native.UnidentifiedSenderMessageContent_Serialize(this);
    }
    contents() {
        return Native.UnidentifiedSenderMessageContent_GetContents(this);
    }
    msgType() {
        return Native.UnidentifiedSenderMessageContent_GetMsgType(this);
    }
    senderCertificate() {
        return SenderCertificate._fromNativeHandle(Native.UnidentifiedSenderMessageContent_GetSenderCert(this));
    }
    contentHint() {
        return Native.UnidentifiedSenderMessageContent_GetContentHint(this);
    }
    groupId() {
        return Native.UnidentifiedSenderMessageContent_GetGroupId(this);
    }
}
export class SessionStore {
}
export var IdentityChange;
(function (IdentityChange) {
    // This must be kept in sync with the Rust enum of the same name.
    IdentityChange[IdentityChange["NewOrUnchanged"] = 0] = "NewOrUnchanged";
    IdentityChange[IdentityChange["ReplacedExisting"] = 1] = "ReplacedExisting";
})(IdentityChange || (IdentityChange = {}));
export class IdentityKeyStore {
    async getIdentityKeyPair() {
        const privKey = await this.getIdentityKey();
        return new IdentityKeyPair(privKey.getPublicKey(), privKey);
    }
}
export class PreKeyStore {
}
export class SignedPreKeyStore {
}
export class KyberPreKeyStore {
}
export class SenderKeyStore {
}
export async function groupEncrypt(sender, distributionId, store, message) {
    return CiphertextMessage._fromNativeHandle(await Native.GroupCipher_EncryptMessage(sender, uuid.parse(distributionId), message, bridgeSenderKeyStore(store)));
}
export async function groupDecrypt(sender, store, message) {
    return Native.GroupCipher_DecryptMessage(sender, message, bridgeSenderKeyStore(store));
}
export class SealedSenderDecryptionResult {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new SealedSenderDecryptionResult(nativeHandle);
    }
    message() {
        return Native.SealedSenderDecryptionResult_Message(this);
    }
    senderE164() {
        return Native.SealedSenderDecryptionResult_GetSenderE164(this);
    }
    senderUuid() {
        return Native.SealedSenderDecryptionResult_GetSenderUuid(this);
    }
    /**
     * Returns an ACI if the sender is a valid UUID, `null` otherwise.
     *
     * In a future release SenderCertificate will *only* support ACIs.
     */
    senderAci() {
        try {
            return Aci.parseFromServiceIdString(this.senderUuid());
        }
        catch {
            return null;
        }
    }
    deviceId() {
        return Native.SealedSenderDecryptionResult_GetDeviceId(this);
    }
}
export class PlaintextContent {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static deserialize(buffer) {
        return new PlaintextContent(Native.PlaintextContent_Deserialize(buffer));
    }
    static from(message) {
        return new PlaintextContent(Native.PlaintextContent_FromDecryptionErrorMessage(message));
    }
    serialize() {
        return Native.PlaintextContent_Serialize(this);
    }
    body() {
        return Native.PlaintextContent_GetBody(this);
    }
    asCiphertextMessage() {
        return CiphertextMessage._fromNativeHandle(Native.CiphertextMessage_FromPlaintextContent(this));
    }
}
export class DecryptionErrorMessage {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static _fromNativeHandle(nativeHandle) {
        return new DecryptionErrorMessage(nativeHandle);
    }
    static forOriginal(bytes, type, timestamp, originalSenderDeviceId) {
        return new DecryptionErrorMessage(Native.DecryptionErrorMessage_ForOriginalMessage(bytes, type, timestamp, originalSenderDeviceId));
    }
    static deserialize(buffer) {
        return new DecryptionErrorMessage(Native.DecryptionErrorMessage_Deserialize(buffer));
    }
    static extractFromSerializedBody(buffer) {
        return new DecryptionErrorMessage(Native.DecryptionErrorMessage_ExtractFromSerializedContent(buffer));
    }
    serialize() {
        return Native.DecryptionErrorMessage_Serialize(this);
    }
    timestamp() {
        return Native.DecryptionErrorMessage_GetTimestamp(this);
    }
    deviceId() {
        return Native.DecryptionErrorMessage_GetDeviceId(this);
    }
    ratchetKey() {
        const keyHandle = Native.DecryptionErrorMessage_GetRatchetKey(this);
        if (keyHandle) {
            return PublicKey._fromNativeHandle(keyHandle);
        }
        else {
            return undefined;
        }
    }
}
function bridgeSessionStore(store) {
    return {
        async storeSession(rawAddress, record) {
            return store.saveSession(ProtocolAddress._fromNativeHandle(rawAddress), SessionRecord._fromNativeHandle(record));
        },
        async loadSession(rawAddress) {
            const pk = await store.getSession(ProtocolAddress._fromNativeHandle(rawAddress));
            return pk ? pk._nativeHandle : null;
        },
    };
}
function bridgeIdentityKeyStore(store) {
    return {
        async getLocalIdentityKeyPair() {
            const keyPair = await store.getIdentityKeyPair();
            return [
                keyPair.privateKey._nativeHandle,
                keyPair.publicKey._nativeHandle,
            ];
        },
        async getLocalRegistrationId() {
            return store.getLocalRegistrationId();
        },
        async saveIdentityKey(name, key) {
            return store.saveIdentity(ProtocolAddress._fromNativeHandle(name), PublicKey._fromNativeHandle(key));
        },
        async isTrustedIdentity(name, key, direction) {
            return store.isTrustedIdentity(ProtocolAddress._fromNativeHandle(name), PublicKey._fromNativeHandle(key), direction);
        },
        async getIdentityKey(name) {
            const key = await store.getIdentity(ProtocolAddress._fromNativeHandle(name));
            return key ? key._nativeHandle : null;
        },
    };
}
export function processPreKeyBundle(bundle, address, localAddress, sessionStore, identityStore, now = new Date()) {
    return Native.SessionBuilder_ProcessPreKeyBundle(bundle, address, localAddress, bridgeSessionStore(sessionStore), bridgeIdentityKeyStore(identityStore), now.getTime());
}
export async function signalEncrypt(message, address, localAddress, sessionStore, identityStore, now = new Date()) {
    return CiphertextMessage._fromNativeHandle(await Native.SessionCipher_EncryptMessage(message, address, localAddress, bridgeSessionStore(sessionStore), bridgeIdentityKeyStore(identityStore), now.getTime()));
}
export function signalDecrypt(message, address, localAddress, sessionStore, identityStore) {
    return Native.SessionCipher_DecryptSignalMessage(message, address, localAddress, bridgeSessionStore(sessionStore), bridgeIdentityKeyStore(identityStore));
}
function bridgePreKeyStore(store) {
    return {
        async storePreKey(id, record) {
            return store.savePreKey(id, PreKeyRecord._fromNativeHandle(record));
        },
        async loadPreKey(id) {
            const pk = await store.getPreKey(id);
            return pk._nativeHandle;
        },
        async removePreKey(id) {
            return store.removePreKey(id);
        },
    };
}
function bridgeSignedPreKeyStore(store) {
    return {
        async storeSignedPreKey(id, record) {
            return store.saveSignedPreKey(id, SignedPreKeyRecord._fromNativeHandle(record));
        },
        async loadSignedPreKey(id) {
            const pk = await store.getSignedPreKey(id);
            return pk._nativeHandle;
        },
    };
}
function bridgeKyberPreKeyStore(store) {
    return {
        async storeKyberPreKey(id, record) {
            return store.saveKyberPreKey(id, KyberPreKeyRecord._fromNativeHandle(record));
        },
        async loadKyberPreKey(id) {
            const pk = await store.getKyberPreKey(id);
            return pk._nativeHandle;
        },
        async markKyberPreKeyUsed(id, ecPrekeyId, baseKey) {
            return store.markKyberPreKeyUsed(id, ecPrekeyId, PublicKey._fromNativeHandle(baseKey));
        },
    };
}
export function signalDecryptPreKey(message, address, localAddress, sessionStore, identityStore, prekeyStore, signedPrekeyStore, kyberPrekeyStore) {
    return Native.SessionCipher_DecryptPreKeySignalMessage(message, address, localAddress, bridgeSessionStore(sessionStore), bridgeIdentityKeyStore(identityStore), bridgePreKeyStore(prekeyStore), bridgeSignedPreKeyStore(signedPrekeyStore), bridgeKyberPreKeyStore(kyberPrekeyStore));
}
export async function sealedSenderEncryptMessage(message, address, senderCert, sessionStore, identityStore) {
    const localAddress = ProtocolAddress.new(senderCert.senderUuid(), senderCert.senderDeviceId());
    const ciphertext = await signalEncrypt(message, address, localAddress, sessionStore, identityStore);
    const usmc = UnidentifiedSenderMessageContent.new(ciphertext, senderCert, ContentHint.Default, null);
    return await sealedSenderEncrypt(usmc, address, identityStore);
}
export function sealedSenderEncrypt(content, address, identityStore) {
    return Native.SealedSender_Encrypt(address, content, bridgeIdentityKeyStore(identityStore));
}
export async function sealedSenderMultiRecipientEncrypt(contentOrOptions, recipients, identityStore, sessionStore) {
    let excludedRecipients = undefined;
    if (contentOrOptions instanceof UnidentifiedSenderMessageContent) {
        if (!recipients || !identityStore || !sessionStore) {
            throw Error('missing arguments for sealedSenderMultiRecipientEncrypt');
        }
    }
    else {
        ({
            content: contentOrOptions,
            recipients,
            excludedRecipients,
            identityStore,
            sessionStore,
        } = contentOrOptions);
    }
    const recipientSessions = await sessionStore.getExistingSessions(recipients);
    return await Native.SealedSender_MultiRecipientEncrypt(recipients, recipientSessions, ServiceId.toConcatenatedFixedWidthBinary(excludedRecipients ?? []), contentOrOptions, bridgeIdentityKeyStore(identityStore));
}
// For testing only
export function sealedSenderMultiRecipientMessageForSingleRecipient(message) {
    return Native.SealedSender_MultiRecipientMessageForSingleRecipient(message);
}
export async function sealedSenderDecryptMessage(message, trustRoot, timestamp, localE164, localUuid, localDeviceId, sessionStore, identityStore, prekeyStore, signedPrekeyStore, kyberPrekeyStore) {
    const ssdr = await Native.SealedSender_DecryptMessage(message, trustRoot, timestamp, localE164, localUuid, localDeviceId, bridgeSessionStore(sessionStore), bridgeIdentityKeyStore(identityStore), bridgePreKeyStore(prekeyStore), bridgeSignedPreKeyStore(signedPrekeyStore), bridgeKyberPreKeyStore(kyberPrekeyStore));
    return SealedSenderDecryptionResult._fromNativeHandle(ssdr);
}
export async function sealedSenderDecryptToUsmc(message, identityStore) {
    const usmc = await Native.SealedSender_DecryptToUsmc(message, bridgeIdentityKeyStore(identityStore));
    return UnidentifiedSenderMessageContent._fromNativeHandle(usmc);
}
export class Cds2Client {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static new(mrenclave, attestationMsg, currentTimestamp) {
        return new Cds2Client(Native.Cds2ClientState_New(mrenclave, attestationMsg, currentTimestamp.getTime()));
    }
    initialRequest() {
        return Native.SgxClientState_InitialRequest(this);
    }
    completeHandshake(buffer) {
        return Native.SgxClientState_CompleteHandshake(this, buffer);
    }
    establishedSend(buffer) {
        return Native.SgxClientState_EstablishedSend(this, buffer);
    }
    establishedRecv(buffer) {
        return Native.SgxClientState_EstablishedRecv(this, buffer);
    }
}
export class HsmEnclaveClient {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    static new(public_key, code_hashes) {
        code_hashes.forEach((hash) => {
            if (hash.length != 32) {
                throw new Error('code hash length must be 32');
            }
        });
        const concat_hashes = Buffer.concat(code_hashes);
        return new HsmEnclaveClient(Native.HsmEnclaveClient_New(public_key, concat_hashes));
    }
    initialRequest() {
        return Native.HsmEnclaveClient_InitialRequest(this);
    }
    completeHandshake(buffer) {
        return Native.HsmEnclaveClient_CompleteHandshake(this, buffer);
    }
    establishedSend(buffer) {
        return Native.HsmEnclaveClient_EstablishedSend(this, buffer);
    }
    establishedRecv(buffer) {
        return Native.HsmEnclaveClient_EstablishedRecv(this, buffer);
    }
}
/**
 * Svr2Client provides functions that manage data sent over the network when
 * comminicating with SVR2 service.
 *
 * Holds an opaque native handle. Use {@link Svr2Client.new} to construct.
 *
 * Interaction with the service is done over a websocket, which is handled by
 * the client. Once the websocket has been initiated, the client establishes a
 * connection in the following manner:
 *
 * 1. Connect to the service websocket, read service attestation message
 * 2. Instantiate the client using {@link Svr2Client.new} with the attestation
 *    message
 * 3. Send the result of {@link Svr2Client.initialRequest}
 * 4. Receive a response and pass it to {@link Svr2Client.completeHandshake}
 *
 * After a connection has been established, a client may send or receive
 * messages. To send a message, they formulate the plaintext, then pass it to
 * {@link Svr2Client.establishedSend} to get the ciphertext message to pass
 * along. When a message is received (as ciphertext), it is passed to
 * {@link Svr2Client.establishedRecv}, which decrypts and verifies it, passing
 * the plaintext back to the client for processing.
 */
export class Svr2Client {
    _nativeHandle;
    constructor(nativeHandle) {
        this._nativeHandle = nativeHandle;
    }
    /**
     * Creates a new instance of the client using the attestation message
     */
    static new(mrenclave, attestationMsg, currentTimestamp) {
        return new Svr2Client(Native.Svr2Client_New(mrenclave, attestationMsg, currentTimestamp.getTime()));
    }
    /** Initial request to send to SVR2, which begins post-attestation handshake. */
    initialRequest() {
        return Native.SgxClientState_InitialRequest(this);
    }
    /**
     * Called by client upon receipt of first non-attestation message from
     * service, to complete handshake.
     */
    completeHandshake(buffer) {
        return Native.SgxClientState_CompleteHandshake(this, buffer);
    }
    /**
     * Encrypts a plaintext message for SVR2
     *
     * Must be called after successfully completing the handshake.
     */
    establishedSend(buffer) {
        return Native.SgxClientState_EstablishedSend(this, buffer);
    }
    /**
     * Decrypts message received from SVR2
     *
     * Must be called after successfully completing the handshake.
     */
    establishedRecv(buffer) {
        return Native.SgxClientState_EstablishedRecv(this, buffer);
    }
}
export var LogLevel;
(function (LogLevel) {
    LogLevel[LogLevel["Error"] = 1] = "Error";
    LogLevel[LogLevel["Warn"] = 2] = "Warn";
    LogLevel[LogLevel["Info"] = 3] = "Info";
    LogLevel[LogLevel["Debug"] = 4] = "Debug";
    LogLevel[LogLevel["Trace"] = 5] = "Trace";
})(LogLevel || (LogLevel = {}));
export function initLogger(maxLevel, callback) {
    let nativeMaxLevel;
    switch (maxLevel) {
        case LogLevel.Error:
            nativeMaxLevel = 1 /* Native.LogLevel.Error */;
            break;
        case LogLevel.Warn:
            nativeMaxLevel = 2 /* Native.LogLevel.Warn */;
            break;
        case LogLevel.Info:
            nativeMaxLevel = 3 /* Native.LogLevel.Info */;
            break;
        case LogLevel.Debug:
            nativeMaxLevel = 4 /* Native.LogLevel.Debug */;
            break;
        case LogLevel.Trace:
            nativeMaxLevel = 5 /* Native.LogLevel.Trace */;
            break;
    }
    Native.initLogger(nativeMaxLevel, (nativeLevel, target, file, line, message) => {
        let level;
        switch (nativeLevel) {
            case 1 /* Native.LogLevel.Error */:
                level = LogLevel.Error;
                break;
            case 2 /* Native.LogLevel.Warn */:
                level = LogLevel.Warn;
                break;
            case 3 /* Native.LogLevel.Info */:
                level = LogLevel.Info;
                break;
            case 4 /* Native.LogLevel.Debug */:
                level = LogLevel.Debug;
                break;
            case 5 /* Native.LogLevel.Trace */:
                level = LogLevel.Trace;
                break;
            default:
                callback(LogLevel.Warn, 'signal-client', 'index.ts', 0, `unknown log level ${nativeLevel}; treating as error`);
                level = LogLevel.Error;
                break;
        }
        callback(level, target, file, line, message);
    });
}
//# sourceMappingURL=index.js.map